<h1>This is the Browse Page</h1>

<h1>Thoughts:</h1>
<ul>
  <% @thoughts.each do |thought| %>
    <%# <li data-thought=<%= thought.id %>
  <%= thought.title %>
  <% if !@thought.is_connected?(thought.id) %>
    <%# <form action="/thoughts/<% @thought.id &>/connect method='post'></form> %>
    <form action="/thoughts/<%= @thought.id %>/connect" method='post'>
      <%# <div class="hidden"> %>
        <input type="text" name='seleted_id' value='<%= thought.id %>' class='' id='parent_thought_connect'>
      <%# </div> %>
      <input type="submit" name='commit' value='Connect' class=''>
    </form>
  <% else %>
    <form action="/thoughts/<%= @thought.id %>/disconnect" method='post'>
      <%# <div class="hidden"> %>
        <input type="text" name='seleted_id' value='<%= thought.id %>' class='' id='parent_thought_disconnect'>
      <%# </div> %>
      <input type="submit" name='commit' value='Disconnect' class=''>
    </form>
  <% end %>
<%# </li> %>
  <% end %>
  <%#= render @thoughts %>
</ul>
